# Normalize all text files to LF in the repository and on checkout,
# regardless of a contributor's local core.autocrlf setting - keeps
# Windows and WSL/Linux checkouts of this repo byte-identical instead of
# depending on whatever each machine happens to have configured.
* text=auto eol=lf
