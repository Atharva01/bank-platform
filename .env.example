DATABASE_URL=postgresql+psycopg://bank:bank@localhost:5432/bank_platform
GROQ_API_KEY=
DEEPSEEK_API_KEY=
# Interim shared-secret gate for admin-only endpoints (DELETE /accounts/{id}) until
# real IAM exists (Phase 6). Set to any random string; send it as the X-Admin-Key header.
ADMIN_API_KEY=
