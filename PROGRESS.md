# Bank Platform — Phase Ledger

Tracks SDLC progress against the phase plan. Update status as work moves through each phase.
See [PROBLEMS.md](PROBLEMS.md) for the log of real issues hit and how they were solved.

Status legend: `Not Started` | `In Progress` | `Blocked` | `Done`

| # | Phase | Status | Started | Completed | Notes |
|---|-------|--------|---------|-----------|-------|
| 0 | Contracts & Interfaces | Done | 2026-09-11 | 2026-09-11 | `AgentType`/`AgentRequest`/`AgentResponse`/`Agent` ABC in `agents.py`. `interfaces.py`'s `MCPClient`/`SessionStore` stubs written but not yet wired in |
| 1 | Agent Business Logic | Done | 2026-09-11 | 2026-09-11 | Real business logic added: atomic balance-linked transactions with overdraft protection, service request status state machine, input validation on account/service creation. Decimal used for money math. 32 pytest tests passing. See Change Log for the full plan that was implemented |
| 2 | MCP Servers | Done | 2026-09-11 | 2026-09-11 | `accounts_server.py`/`transactions_server.py`/`service_server.py` now own their data + invariants (validation, atomicity, status state machine — moved out of `agents.py`). `InProcessMCPClient` in `interfaces.py` dispatches `call_tool(name, params)`. Agents no longer touch Postgres/SessionLocal at all. Also closed a gap: transaction update/delete now atomically re-adjust Account.balance. 36 pytest tests passing, verified over real HTTP. Transport is in-process (no subprocess/real MCP protocol) — deliberate, see Change Log |
| 3 | LLM Integration | In Progress | 2026-09-11 | | Core build done: `graph.py` wires a LangGraph supervisor (`langgraph-supervisor`) to 3 specialist sub-agents (`create_agent()` per domain — `create_react_agent` is deprecated in langchain 1.x), each bound only to its own domain's tools. LLM client (`llm.py`): OpenAI-compatible shape via `langchain_openai.ChatOpenAI`, provider went Ollama → DeepSeek → **Groq (`openai/gpt-oss-20b`)** after both turned out to be "thinking" models that broke the actual multi-turn supervisor handoff (see PROBLEMS.md #10). `main.py` has a new `ChatRequest{session_id, message}`/`ChatResponse{reply}` contract. `agents.py`/`coordinator.py` deleted; `interfaces.py` trimmed to just the `SessionStore` stub. A live demo caught a real duplicate-deposit bug (two compounding causes: retry-restart replaying a committed tool call, and the supervisor spontaneously re-delegating a completed request) — fixed with checkpoint-based resume, tightened prompts, and a deterministic per-thread idempotency guard on all `create_*` tools (see PROBLEMS.md #13). Supervisor prompt rewritten to be explicit about each sub-agent's job and limits, with a rule to ask for clarification instead of guessing on unclear requests. Added REST resource endpoints (`accounts_router.py`/`transactions_router.py`/`service_router.py`) so `/chat` is no longer the only entry point — deterministic CRUD now bypasses the LLM entirely and gets real HTTP status codes (404/400/409/422) via global exception handlers. 35 tests passing, verified end-to-end over real HTTP across all 3 domains via both `/chat` and the new REST endpoints. Remaining: no formal evaluation of routing accuracy across ambiguous/multi-intent inputs (prompt improved but unmeasured) |
| 4 | Session Store | Done | 2026-09-11 | 2026-09-11 | `session_id` now genuinely means something: `graph.py` uses it directly as the LangGraph `thread_id` (was a fresh throwaway UUID per request before). Conversation history persisted via `PostgresSaver` (`langgraph-checkpoint-postgres`), replacing `InMemorySaver` — real multi-turn memory, and this is what actually fixes the unbounded-RAM growth flagged as a priority item last session. Idle sessions (10 min, decided with the user) are lazily expired per-request — no scheduler exists yet — cleaning up the checkpoint thread, `tool_utils._completed_calls`, and the new `sessions` table row together. `session_store.py` implements `interfaces.py`'s `SessionStore` ABC for real (Postgres-backed `get`/`update`, merge semantics) for "inter-agent shared state" — built per the diagram, no agent consumes it yet by design. Caught and fixed a real bug while verifying live: `_extract_reply()` searched the *entire* persisted history, which was safe when threads were single-use but silently risked surfacing a stale earlier turn's answer once threads persist across calls — fixed by slicing to only messages added by the current invocation. See PROBLEMS.md #15 |
| 5 | PII Redaction | Done | 2026-09-11 | 2026-09-11 | Reversible tokenization between the agent layer and the LLM (`pii_guard.py`) - real values swapped for stable per-conversation tokens before a tool result reaches the LLM, swapped back before a tool executes and in the final reply. Scope deliberately narrow after live testing: only `Account.owner_name`, not IDs/balances/amounts/free-text fields - broader tokenization measurably increased tool-call hallucination (see PROBLEMS.md #21). Evaluated and rejected LangChain's built-in `PIIMiddleware` first - its strategies are all irreversible, incompatible with data the assistant needs real values of to function |
| 6 | Auth & Authorisation | Done (staff + customer) | 2026-09-11 | 2026-09-12 | Real staff authentication (`auth.py`/`auth_router.py`) replaces `admin_auth.py`'s static shared-secret stopgap: `StaffUser` table (hashed passwords via `bcrypt`), `POST /auth/login` issues a signed JWT (`pyjwt`, 60-minute expiry), `get_current_staff_user` re-fetches the real user on every request so a removed staff account stops working immediately. Gates `DELETE /accounts/{id}`, the one elevated action that exists today. **2026-09-12: customer-facing auth added, reversing the earlier "staff/admin only" scoping** at explicit user direction after the Phase 7 deviation review flagged it as unacceptable — any anonymous caller could act on any account_id it named, over both `/chat` and REST. New `Customer` model + `customer_router.py` (`/customers/register`, `/customers/login`), a nullable `Account.customer_id` FK (one customer, many accounts), and `authz.py` enforcing ownership everywhere — REST (`accounts_router.py`/`transactions_router.py`/`service_router.py`) and chat tools alike (`owner_guard`/`inject_customer_id` wrappers, same `RunnableConfig`-injection technique as `pii_guard`/`idempotent`). JWTs now carry a `"typ"` claim (`staff`/`customer`) so the two token kinds can't pass for each other. `graph.py`/`session_store.py`: a client-chosen `session_id` is now bound to the first customer who uses it — a different customer reusing the same id gets `403`, closing a session-hijack gap in the previously-unconsumed shared-state store. New `list_accounts` chat tool + `GET /accounts` REST endpoint so a customer isn't forced to already know an account id. Does **not** resolve the deeper `owner_name` identity-redesign question — still separately deferred (see account-schema-redesign-deferred memory). 80 tests passing (11 new). Verified live: register/open-account/no-token-401/cross-customer-404/list-scoping, plus one `/chat` exchange per customer confirming `list_accounts` answers "what's my balance" with no account id stated, and confirming session_id reuse across customers is rejected (403). **2026-09-13: closed a real gap surfaced by live use** — ownership alone let any customer self-approve their own `ServiceRequest` (`pending` → `approved` → `completed`) via both REST and chat. `status` removed entirely from the customer-facing PATCH/chat tool; a new staff-only `PATCH /staff/service-requests/{id}/status` (gated by `get_current_staff_user`) is now the only path that can change status. See PROBLEMS.md #25 |
| 7 | Edge Layer | Partial (Traefik built, not yet live-verified) | 2026-09-12 | | In-app rate limiting shipped (`slowapi`): 5/minute per IP on the auth/registration endpoints, 20/minute on `/api/chat`. **2026-09-13: deployment target decided (bare VPS, self-managed Docker Compose)** and Traefik built following FastAPI's own official reference architecture (`full-stack-fastapi-template`) - external `traefik-public` network, single-domain path-based routing (every backend route moved under `/api`, same-origin with the frontend, no CORS needed in production), security-headers + centralized edge rate-limiting middleware (fixes `slowapi`'s known in-memory/per-process limitation - kept as a redundant inner layer, not removed), ACME `tlsChallenge` for Let's Encrypt with placeholder domain/email. New `Dockerfile` (backend, `fastapi run` not `fastapi dev`) + `docker-compose.prod.yml` (additive - `docker-compose.yml` stays the local-dev file). Found and fixed a real prerequisite bug: `database.py`'s `DATABASE_URL` was a hardcoded literal, never actually read from `.env`'s own line. **Not yet verified live** - Traefik's Docker-provider routing can't be exercised on this Windows/Docker Desktop dev machine (a client-library incompatibility with Docker Desktop's socket forwarding, isolated and confirmed not a config issue - see PROBLEMS.md #24); real verification (routing, security headers, edge rate limit, real TLS) needs the actual VPS. WAF (CrowdSec/ModSecurity) and true DDoS mitigation remain explicitly out of scope, deferred per an earlier decision |
| 8 | Observability & Cost Tracker | Done (call-metadata scope) | 2026-09-13 | 2026-09-13 | New `AgentEventLog` table + `observability.py`'s `ObservabilityCallbackHandler` (a standard LangChain `BaseCallbackHandler`, the same extension point LangSmith tracing itself uses) records one row per LLM call and per tool call - duration, token usage, an estimated cost where the model's rate is known, success/failure - attached once via `graph.py`'s `config["callbacks"]`, no changes needed to `llm.py`/any `*_tools.py`/the agents themselves. Deliberately does **not** log raw prompt/reply/tool-arg content (that's where real account data lives, same reasoning as Phase 5's PII scope) and does **not** attempt host CPU/memory/disk metrics (needs a real deployment target first, not faked). New staff-gated `GET /observability/usage` (`observability_router.py`) aggregates totals + a by-day breakdown. Two things verified empirically rather than assumed, per plan: (1) LangChain does **not** pass `metadata` to `*_end`/`*_error` callbacks, only `*_start` - fixed by capturing context once at start, keyed by `run_id`; (2) `agent_name` attribution is **not reliably available** - a real live call recorded `agent_name: None` on every row, so that column stays unpopulated in practice, not a future reporting dimension, without deeper LangGraph internals work. The same live call's first real data also surfaced a genuine anomaly (an 85.7s LLM call despite `llm.py`'s 30s client timeout) - see PROBLEMS.md #23. Muse Spark has no cost-per-token entry (unknown/unconfirmed rate - token counts are still recorded precisely; Groq's known historical rate is included for if/when it's active again). 10 new tests (`tests/test_observability.py`, zero LLM cost - synthetic LangChain objects fed directly to the handler, plus a REST test), 96 passing total |
| 9 | Agent Evaluation Suite | Not Started | | | Eval harness across both LLM backends, CI-gated |
| 10 | Integration & Hardening | Not Started | | | End-to-end tests, load testing, security review |

## Current Focus

**Phase 7 — Traefik edge layer built (bare VPS + Docker Compose target), not yet live-verified.** Phases 0-2, 4-6, 8 Done; Phase 7 Partial - rate limiting done, Traefik/security-headers/edge-rate-limit built following FastAPI's own reference architecture but unverified locally (Windows/Docker Desktop limitation, see PROBLEMS.md #24) - real verification needs the actual VPS; WAF/true-DDoS still explicitly deferred. Phase 3 functionally complete but not formally closed (see its own row). Phase 9 (Agent Evaluation Suite) and Phase 10 (Integration & Hardening) not started.

**2026-09-13: service-request self-approval gap closed (Phase 6 follow-up).** Live use surfaced a real authorization hole - Phase 6's customer auth enforced ownership on `ServiceRequest` status changes but not *who* may make one, so any customer could approve their own request. Fixed by moving `status` off the customer-facing PATCH/chat tool entirely and adding a staff-only `PATCH /staff/service-requests/{id}/status` endpoint. See PROBLEMS.md #25, Phase 6's row for detail.

**Final shape:**

```
main.py
  -> /chat: ChatRequest{session_id, message} -> ChatResponse{reply}     [conversational, always 200]
       -> graph.py: run() -> sanitize_incoming() -> supervisor.invoke() against a checkpointed thread,
            retrying by RESUMING (not restarting) on a retryable failure -> detokenize() the final reply
            -> LangGraph supervisor (langgraph-supervisor, PostgresSaver checkpointer, thread_id = session_id)
                 -> accounts_agent / transaction_agent / service_agent (create_agent() per domain)
                      -> accounts_tools.py / transactions_tools.py / service_tools.py (StructuredTool wrappers)
                           -> pii_guard.pii_guard() tokenizes Account.owner_name only (accounts_tools.py)
                           -> tool_utils.idempotent() guards create_account/create_transaction/create_service_request
  -> /accounts, /accounts/{id}/transactions, /transactions/{id},        [deterministic REST, no LLM,
     /accounts/{id}/service-requests, /service-requests/{id}            real 404/400/409/422]
       -> accounts_router.py / transactions_router.py / service_router.py
            -> global exception handlers map exceptions.py -> HTTP status + {detail, error_type}
                                     |
                                     v  (both paths converge here)
                      accounts_server.py / transactions_server.py / service_server.py (UNCHANGED business logic)
                           -> crud_*.py -> database.py / models.py -> Postgres
```

**LLM client:** Meta Muse Spark (`muse-spark-1.3-contributor`) — `llm.py`, OpenAI-compatible shape (`base_url`+`api_key`), provider history: Ollama → DeepSeek → Groq (`openai/gpt-oss-20b`) → Muse Spark. See PROBLEMS.md #10/#12/#16-#20 for the full trail.

**Resolved for the REST surface:** `/chat` was the *only* functional endpoint — every request, including fully deterministic CRUD, had to go through the LLM, and that's also why `/chat` could only ever return `200` (natural-language replies can't carry a reliable status code). Added REST endpoints per domain that call the same business-layer functions directly, no LLM in the loop, with real HTTP status codes (404/400/409/422) via global exception handlers and a machine-readable `error_type` field. `/chat` itself is untouched and still communicates errors conversationally inside `reply` — correct for a chat endpoint, and no longer the *only* option for a caller that needs structured errors.

**Also noticed, not fixed (pre-existing, out of scope):** `delete_account` doesn't check for or cascade related transactions/service_requests — deleting an account leaves orphaned rows referencing a nonexistent `account_id`.

**Correctness fix from a live demo (PROBLEMS.md #13):** a manual demo request produced a triple deposit — traced to two compounding bugs (retry-restart replaying an already-committed tool call, and the supervisor occasionally re-delegating a completed request to the same sub-agent). Fixed at three layers: checkpoint-based resume (`InMemorySaver` + per-request `thread_id`, resume on retry instead of restart), tightened supervisor/sub-agent prompts (measured: double-delegation dropped from ~1/10 to 0/20 trials), and a deterministic `tool_utils.idempotent()` guard on the three `create_*` tools that rejects a duplicate `(tool, args)` call within the same conversation thread regardless of why the LLM issued it twice — verified directly at the tool layer without needing an LLM call. `tenacity` dependency removed (retry logic is now the manual resume loop in `graph.py`).

**delete_account isolated from agents/chat:** closing an account isn't a customer/agent self-service action — no IAM/elevated-access layer exists yet (Phase 6) to gate it. Removed `delete_account` from `ACCOUNTS_TOOLS` so no sub-agent can reach it via `/chat`; updated `accounts_agent`'s and the supervisor's prompts to say so explicitly rather than let the LLM attempt a workaround. The REST `DELETE /accounts/{id}` endpoint stays in `accounts_router.py` (a future admin caller needs a way in once real access control exists) but is explicitly documented as NOT currently access-controlled — isolating it from the agent path was the fix in scope, locking down who can call it directly is separate, unstarted work. Also noted: no "apply for closure" path exists either (`service_agent`'s request-type allowlist has no closure type) — a real gap, flagged for whoever builds the admin/IAM layer, not built here.

**Routing-accuracy evaluation reclassified:** moved out of "Phase 3 remaining" — a real evaluation (does the LLM route ambiguous/multi-intent input correctly) needs a live-model harness and belongs in Phase 9 (Agent Evaluation Suite, not started), not folded into this phase's exit criteria. In the meantime, all four prompts (`ACCOUNTS_AGENT_PROMPT`/`TRANSACTION_AGENT_PROMPT`/`SERVICE_AGENT_PROMPT`/`SUPERVISOR_PROMPT`) were pulled out to named constants in `graph.py`, with `tests/test_supervisor_prompt.py` asserting on their content (every agent named, anti-re-delegation rule present, clarification fallback present) — zero LLM calls, cheap to run in CI, but only a regression guard on the prompt text, not a verification that the model actually follows it.

**DeepSeek re-evaluated as a Groq fallback, rejected again:** researched current DeepSeek model names (`deepseek-v4-flash` is a retired alias; the live model is `DeepSeek-V4.1-Flash`, served under the model string `deepseek-flash`). A standalone call looked clean (no `reasoning_content` in the response), but testing against the *real* multi-turn supervisor flow — the same methodology that caught this the first time (PROBLEMS.md #10) — reproduced the identical failure: `reasoning_content` must be echoed back on the second turn, which breaks LangGraph's supervisor handoff. Confirms this isn't a one-off from before; the current DeepSeek Flash model still isn't viable for this architecture. Reverted to Groq. Total cost of checking: 2 live calls. Worth revisiting only if DeepSeek ships a non-thinking model variant, or if `langchain_openai.ChatOpenAI` gains native `reasoning_content` pass-back support.

**Not yet done in this phase:** ambient rate-limit handling for Groq's free tier is just `max_tokens=500`, not a real backoff strategy (and Groq's *daily* token cap was hit repeatedly during this session's testing — the free tier is tight enough that any serious further testing needs either a paid tier or very sparing use of live calls).

**Unbounded in-memory state — resolved by Phase 4, not here:** the in-memory checkpointer and idempotency dedup dict growing forever was flagged as a priority item; the actual fix landed as part of building the Session Store (Phase 4, see its own row and PROBLEMS.md #15) rather than as a standalone patch, since a real session lifecycle was needed either way.

**Session Store's touch/expiry functions are already a real consumer** — `session_store.touch()`/`get_last_activity()`/`delete()` are called on every `/chat` request and by the new sweep (below); only the "inter-agent shared state" half (`get`/`update`) has no consumer, which is the accepted, explicitly-chosen gap from Phase 4 planning, not a new problem.

**Idle-session sweep added (closes the last Phase 4 residual gap):** `graph.py`'s `sweep_expired_sessions()` proactively cleans up any session past the 10-minute idle timeout, not just the one in the current request — closes the "abandoned session lingers forever" gap noted when Phase 4 shipped. Runs on a timer via a FastAPI `lifespan` background `asyncio` task in `main.py` (interval = the idle timeout itself), no new dependency. `_expire_if_idle`/`_expire_session` refactored so both the per-request and periodic paths share one cleanup routine.

**DeepSeek rejected a third time, more decisively:** re-tested with `extra_body={"thinking": {"type": "disabled"}}` (the missing piece in both prior attempts) — this does stop the `reasoning_content` crash, but a live multi-turn supervisor run silently hallucinated a completed account-opening with nothing written to the database. Worse failure mode than a crash. Reverted to Groq. See PROBLEMS.md #17. 2 live calls spent.

**Meta Muse Spark validated, then switched to as the active provider:** `muse-spark-1.3-contributor` (Meta Model API, OpenAI-compatible) passed the same real multi-turn supervisor test DeepSeek failed three times — genuine tool calls, correct DB writes, no hallucination, verified independently against the database. One real issue hit and fixed along the way: it's more verbose than `gpt-oss-20b` and needs `max_tokens=2048` (not 500) to avoid truncated replies. See PROBLEMS.md #18. Initially left Groq active (cost tradeoff, not correctness) — then Groq's known "transient" tool-call parse failure (#12) actually exhausted its 3-attempt retry budget on a real request through the new chat UI, surfacing as a raw 500. Switched the active provider to Muse Spark on the spot rather than tune Groq's retry/backoff further, confirmed live via `/chat`. See PROBLEMS.md #19. 5 live calls spent total across all three rounds.

**`delete_account` hardened:** cascades to delete related `Transaction`/`ServiceRequest` rows (was leaving orphans), and the REST `DELETE /accounts/{id}` endpoint is now gated by `admin_auth.require_admin` — a static shared-secret header (`X-Admin-Key`/`ADMIN_API_KEY`) standing in for real IAM (Phase 6, not started). See PROBLEMS.md #16.

**Routing-accuracy evaluation:** deferred again, explicitly — see Backlog section above.

## Backlog (not started, explicitly deferred)

- Routing-accuracy evaluation across ambiguous/multi-intent input — real
  work belongs in Phase 9 (Agent Evaluation Suite, needs a live-model
  harness); deferred again on explicit request, still just the cheap
  structural prompt test (`tests/test_supervisor_prompt.py`) for now.

## Change Log

- 2026-09-11 — Ledger created. Phase plan agreed; Python stack, LLM-based Coordinator routing decided.
- 2026-09-11 — Phases 0 and 1 built in a CRUD-first detour (agents talk directly to Postgres, bypassing the MCP abstraction) to prove out agent coordination end-to-end before returning to the phased plan. Marked Done retroactively. Phase 2 is now the gap between current state and the original architecture diagram.
- 2026-09-11 — Reopened Phase 1: CRUD passthrough alone isn't real agent business logic. Agreed a plan (balance linkage, overdraft rule, service status machine, input validation, atomic commits) before moving on to Phase 2.
- 2026-09-11 — Discussed tech stack for the business-logic work: plain Python classes (no agent framework yet), hand-rolled state machine, and switching balance/amount math from float to Decimal to avoid precision drift.
- 2026-09-11 — Discussed MCP server design ahead of Phase 2: each MCP server is a domain-owning microservice (not middleware) that will own its own DB access once retrofitted. Agreed each logical write should be one composite crud_* function (e.g. `create_transaction_and_update_balance`), not multiple functions orchestrated by the agent, so it maps cleanly onto a single future MCP tool call and stays atomic. Transport: in-process first; real MCP later via stdio/Streamable HTTP, not WebSocket.
- 2026-09-11 — Implemented step 2 (atomic balance-linked transaction writes with overdraft protection, Decimal math), step 3 (service status state machine), and step 4 (input validation on account/service creation). Phase 1 marked Done. 32 pytest tests passing. Phase 2 (MCP Servers) is now current focus.
- 2026-09-11 — Planned Phase 2 in detail. Refined the ownership principle: MCP servers own their own data invariants (validation + atomicity + state transitions) rather than trusting the caller, so input validation and the status state machine move from `agents.py` into the MCP servers. Also decided transaction.update/delete will atomically re-adjust Account.balance once the Transactions server formally owns that invariant, closing a previously-flagged gap.
- 2026-09-11 — Considered adopting the real `mcp` SDK (FastMCP, stdio/in-memory transport) instead of hand-rolling. Decided against it for now: the SDK is async-only, which would ripple through agents/coordinator/endpoint/tests, plus subprocess management adds real complexity (and Windows-specific risk for stdio) for zero functional gain while the only caller is our own Coordinator in the same process. The `MCPClient` interface is the seam that keeps this swappable later without touching agent code, once an external caller actually exists — deferred, not rejected.
- 2026-09-11 — Implemented Phase 2: three MCP server modules now own their domain's data and invariants; `InProcessMCPClient` dispatches tool calls; agents.py no longer touches Postgres/SessionLocal at all. Closed the transaction-edit/delete balance-staleness gap in the same pass. 36 tests passing, verified over real HTTP. Phase 2 marked Done.
- 2026-09-11 — Restructured the project: all root-level modules moved into `src/bank_platform/` as a real installable package (hatchling build backend, `uv sync` installs it editable), replacing scattered root files. All internal imports rewritten to package-qualified (`from bank_platform.x import y`). Tests updated the same way. 36 tests still passing, verified over real HTTP against the new layout.
- 2026-09-11 — Flagged UI/UX-facing backend gaps ahead of any frontend work. Fixed the two cheap/foundational ones now: `/chat` maps `AgentResponse.error` to real HTTP status codes (404/400/422) instead of always returning 200, and CORS middleware was added (permissive for now, tighten before real deployment). Streaming, session persistence, and auth are already covered by Phases 3/4/6 — not missing, just not yet built.
- 2026-09-11 — LangChain/LangGraph mandated for Phase 3 (decided, not debated), with full agentic tool-calling scope (not just intent classification): a LangGraph supervisor (via `langgraph-supervisor`) delegates to 3 specialist sub-agents, each bound to its own domain's tools. LLM client is OpenAI-compatible-API-shaped (`langchain_openai.ChatOpenAI` with `base_url`/`api_key`) so swapping providers is a config change, not a code change — self-hosted via Ollama running `qwen3.5:4b` for now. Audited the actual codebase (not assumptions) to scope the replacement: `agents.py`/`coordinator.py`/`InProcessMCPClient`'s dispatch are removed; the three MCP server modules keep every line of validation/atomicity/state-machine logic unchanged (only additive type hints/docstrings, no LangChain import added there); a new `tools.py` wraps them via `StructuredTool.from_function(..., handle_tool_error=True)` so existing domain exceptions get surfaced to the LLM with no new exception-handling code; most of the 36 tests survive by testing server functions directly instead of through the removed agent classes.
- 2026-09-11 — Added type hints + docstrings to all 13 server functions (additive only, verified zero behavior change via full test suite). Built `tool_utils.tool_safe()` + `accounts_tools.py`/`transactions_tools.py`/`service_tools.py`, wrapping all 13 functions as LangChain `StructuredTool`s — discovered along the way that `handle_tool_error=True` only catches LangChain's own `ToolException`, not our domain exceptions, so `tool_safe()` translates them first. Verified tool schema inference and a full create/read/delete round trip against real Postgres.
- 2026-09-11 — Ditched Ollama for prototyping in favor of DeepSeek (`deepseek-flash`) — cheap and explicitly optimized for agentic tool-calling. Confirmed via official docs: OpenAI-compatible endpoint at `https://api.deepseek.com`. Built `llm.py` (`ChatOpenAI` + `.env`/`python-dotenv` for the key). Verified against the live API: plain completions work, and `bind_tools(ACCOUNTS_TOOLS)` correctly generates well-formed, accurately-argued tool calls. Also caught and fixed a real gap in the same pass: `.env` existed but wasn't in `.gitignore` — fixed before any secret was staged (see PROBLEMS.md #9).
- 2026-09-11 — DeepSeek broke on the actual multi-turn supervisor flow (reasoning_content pass-back requirement). Tried Groq-hosted `qwen/qwen3.6-27b` next — avoided that error but leaked `<think>` blocks into content regardless of `/no_think`. Switched to `openai/gpt-oss-20b` on Groq — verified clean against the real failure scenario (full supervisor + sub-agent graph, tool call, multi-turn handoff and back). See PROBLEMS.md #10.
- 2026-09-11 — Built `graph.py`: `create_agent()` per domain (Accounts/Transaction/Service, each bound to its own tools) wired together via `langgraph-supervisor`'s `create_supervisor()`. Found and fixed two real issues while building it: the supervisor's post-handback message isn't reliably the actual answer (run-to-run variance — fixed via `_extract_reply()`'s "last message with content and no pending tool_calls" heuristic, not "last message"; PROBLEMS.md #11), and `gpt-oss-20b` occasionally emits a tool-call payload Groq's parser can't parse — confirmed transient via direct retries, fixed with a `tenacity` retry around the whole `supervisor.invoke()` call rather than wrapping the raw LLM client (which breaks `.bind_tools()`; PROBLEMS.md #12). Deleted `agents.py`/`coordinator.py`, trimmed `interfaces.py` to just `SessionStore`, new `ChatRequest`/`ChatResponse` contract in `main.py`. Repointed all business-rule tests to call server functions directly (26 tests, same coverage minus the now-obsolete intent/routing tests). Verified end-to-end over real HTTP across all 3 domains plus an insufficient-funds error path.
- 2026-09-11 — Ran a manual demo over real HTTP and caught a genuine correctness bug: a single "deposit" request landed three transactions. Traced with `output_mode="full_history"` (default `"last_message"` hides exactly this) to two compounding causes — the #12 retry restarting the whole graph and replaying an already-committed tool call, and the supervisor independently re-delegating an already-completed request to the same sub-agent (~1/10 baseline rate, unrelated to any error). Fixed at three layers: (1) `graph.py` now compiles with an `InMemorySaver` checkpointer and resumes (`input=None`, same `thread_id`) instead of restarting on retry, removing the `tenacity` dependency in favor of a manual resume loop; (2) tightened the supervisor's and `transaction_agent`'s prompts against redundant re-delegation, measured to drop double-delegation to 0/20 trials; (3) added `tool_utils.idempotent()`, a deterministic per-thread dedup guard on `create_account`/`create_transaction`/`create_service_request` (the only tools that insert new rows and are therefore unsafe to repeat — `update`/`delete` are already naturally idempotent here), using `RunnableConfig` injection so the dedup key never reaches the LLM-visible tool schema. Verified the guard directly at the tool layer (duplicate call same thread → rejected; same args different thread → executes normally) with zero LLM calls. Also hit Groq's *daily* token cap mid-investigation, which capped how much further live-model verification was practical this session. See PROBLEMS.md #13.
- 2026-09-11 — Rewrote the supervisor's prompt to be explicit and authoritative about each sub-agent's job AND limits (previously a loose keyword list), plus an explicit rule to ask for clarification rather than guess/delegate on an unclear or out-of-domain request — directly targets the ambiguous/multi-intent routing gap noted above, though it's a prompt strengthening, not a formal evaluation (that item is still open).
- 2026-09-11 — Flagged and fixed a real architectural gap: `/chat` was the *only* functional endpoint, making the LLM a mandatory gateway for fully deterministic CRUD (opening an account, listing transactions). Added `accounts_router.py`/`transactions_router.py`/`service_router.py` — one REST router per domain, each calling the existing `*_server.py` business functions directly (same business layer `/chat` uses, no LLM involved), plus global FastAPI exception handlers mapping `exceptions.py`'s taxonomy to real HTTP status codes (404/400/409/422) with a machine-readable `error_type` field. This also resolves the "conversational-only errors" trade-off noted above, for the REST surface — `/chat` itself is unchanged. 9 new tests (`tests/test_rest_endpoints.py`, FastAPI `TestClient`, zero LLM calls) plus a live HTTP smoke test, both passing. Deliberately out of scope for this pass: no list-all endpoints where no business function backs them, no new middleware beyond existing CORS (rate limiting/observability belong to Phases 7/8).
- 2026-09-11 — Four follow-ups worked in one pass. (1) Routing-accuracy evaluation reclassified out of Phase 3's exit criteria into Phase 9 (needs a live-model harness); added a zero-LLM-call structural regression test on prompt content instead (`tests/test_supervisor_prompt.py`), which required pulling all four agent/supervisor prompts out to named constants in `graph.py` first. (2) Re-tested DeepSeek as a Groq fallback per explicit request — researched current model naming (`deepseek-v4-flash` -> `deepseek-flash`/DeepSeek-V4.1-Flash), then verified against the real multi-turn supervisor flow (not just a standalone call, learned from PROBLEMS.md #10) and got the identical `reasoning_content` failure as before. Rejected again, reverted to Groq; 2 live calls spent total. (3) `delete_account` removed from `ACCOUNTS_TOOLS` so it's unreachable via `/chat` — closing an account needs elevated access that doesn't exist yet (Phase 6). The REST `DELETE /accounts/{id}` endpoint was deliberately kept (not removed) so a future admin caller has a path in, explicitly documented as not yet access-controlled. (4) Unbounded in-memory state (`InMemorySaver` checkpointer + idempotency dedup dict) confirmed as a priority item — strategy being planned before implementation, not yet coded.
- 2026-09-11 — Built Phase 4 (Session Store) after the user pointed back at the reference diagram's "Session Store" box (conversation history + inter-agent shared state) — directly tying into the just-flagged unbounded-state problem, since `session_id` was dead on arrival (`ChatRequest` accepted it, `graph.run()` never used it, generating a throwaway UUID per request instead). Planned via EnterPlanMode; the user set the idle-session TTL at 10 minutes ("banking platforms don't expect hours of back-and-forth... limited scope of operations") and chose to build the shared-state store without a consumer yet. Implementation: `session_id` is now the LangGraph `thread_id` directly; swapped `InMemorySaver` for `langgraph-checkpoint-postgres`'s `PostgresSaver` (new dependency), bootstrapped via `.setup()` at import time; added `session_store.py` implementing `interfaces.py`'s `SessionStore` (new `Session`/`sessions` table, merge-semantics `update()`); idle sessions are expired lazily per-request (no scheduler exists), cleaning up the checkpoint thread, `tool_utils._completed_calls`, and the `sessions` row together. Caught a real bug while doing the live continuity check (2 calls spent, then hit Groq's daily cap again): `_extract_reply()` searched the *entire* persisted history in reverse, which was correct when every thread was single-use but risked silently surfacing a stale earlier turn's answer once threads persist — fixed by slicing to only messages added by the current invocation before extracting. Verified everything else directly (session_store CRUD, merge semantics, expiry logic including the not-yet-expired case) with zero LLM calls. 42 tests passing throughout, no regressions. See PROBLEMS.md #15.
- 2026-09-11 — Follow-up on #15: before accepting "extraction-layer bug" as the full explanation, ruled out a more concerning alternative — a reported (unconfirmed by maintainers) upstream issue where `PostgresSaver` can fail to load full message history from checkpoints, which could produce the same symptom for a completely different reason. Two zero-LLM-call checks ruled it out for this codebase: the original test's stale reply already contained the exact correct transaction ID from turn 1 (only possible if full history reached the model), and driving `supervisor.update_state()` through four sequential writes on one thread came back complete and correctly ordered via `get_state()`. Root cause confirmed as extraction-layer only. See PROBLEMS.md #15.
- 2026-09-11 — Live-tested Meta Muse Spark (`muse-spark-1.3-contributor`, Meta Model API) as a Groq fallback candidate, prompted by "gpt-oss works, so try Meta Muse too." Ran the same real multi-turn supervisor test that caught DeepSeek's failures — passed cleanly: genuine tool calls, correct DB writes, no hallucination. One real issue hit and fixed along the way (not a rejection): `max_tokens=500` was too tight for this more-verbose model and truncated replies (`finish_reason: "length"`, diagnosed for free via `supervisor.get_state()`); raised to 2048 and re-confirmed. Documented in `llm.py` and PROBLEMS.md #18 as a validated fallback, but left Groq as the active provider — a cost tradeoff (contributor tier is paid, ~$20/mo budget vs. Groq's free tier), not a correctness one. 4 live calls total, run in two precise, budget-conscious rounds per explicit direction.
- 2026-09-11 — Built Phase 6 (Auth & Authorisation), scoped to staff/admin only. Planned via EnterPlanMode: explored the codebase first and confirmed there's genuinely zero identity substrate anywhere (no User/Customer table, `Account.owner_name` is a free string, `session_id` is a pure conversation-continuity token) - real *customer* auth would mean designing that identity model now, directly colliding with the deferred account-schema-redesign decision. Scoped Phase 6 to staff/admin auth only on your direction, leaving customer-facing `/chat` and the REST surface exactly as open as before. Built: `StaffUser` model (id/username/hashed_password, no roles - one elevated action exists today), `auth.py` (bcrypt password hashing, JWT issuance/validation via `pyjwt`, `get_current_staff_user` dependency that re-fetches the real user every request so a removed account stops working immediately), `auth_router.py` (`POST /auth/login`, same-error-for-both-failure-modes to prevent username enumeration), `scripts/create_staff_user.py` (one-off bootstrap, no public self-registration). `admin_auth.py` and `ADMIN_API_KEY` retired entirely - `DELETE /accounts/{id}` now gated by `get_current_staff_user`. 8 new tests (`tests/test_auth.py`), 67 passing total, verified live end-to-end via curl (401 no-token/garbage-token/wrong-password, 200 real-token-delete, 404 confirm-gone).
- 2026-09-11 — Built Phase 5 (PII Redaction). Planned via EnterPlanMode: researched LangChain's built-in `PIIMiddleware` first (reuse over reinvention), rejected it - all its strategies (redact/mask/hash/block) are irreversible, and almost everything in this app's PII scope is functionally load-bearing (the assistant needs a real account ID to call the right tool). Built custom reversible tokenization instead (`pii_guard.py`): a per-thread real-value<->token map, a `pii_guard()` decorator composed as the outermost layer around each tool (detokenizes args before the real call, tokenizes the result after), a known-value substitution pass on incoming messages, and final-reply detokenization in `graph.py`. Initially scoped broadly (IDs, names, balances, amounts, free-text fields across all 3 domains) - live verification caught a real problem: 2 of 3 test runs hallucinated a completed action with no actual tool call, traced to the tokenized context looking too templated for the model to reason over reliably (not a bug in the mechanism itself - schema/description integrity and correct token round-tripping were both independently verified). Narrowed scope drastically on the user's direction ("keep a light foot on redactions"): only `Account.owner_name` is tokenized now; `transactions_tools.py`/`service_tools.py` have no pii_guard wrapping at all, since neither domain has an equivalent personal-name field. Re-verified live with the identical flow - clean success. See PROBLEMS.md #21. 16 new tests (`tests/test_pii_guard.py`), 62 passing total.
- 2026-09-11 — Re-verifying the Muse Spark switch with a curl repro of the UI failure surfaced three unrelated bugs (PROBLEMS.md #20): `fastapi dev --reload` silently hangs after editing `llm.py` (fixed by always killing+restarting instead of trusting reload), `max_tokens=2048` was still too low for a bare-account-ID message (raised to 4096), and Docker Desktop's engine crashed mid-session with zero timeout anywhere in the stack to catch it - a dead Postgres could hang a `/chat` request forever with no error. Added `timeout=30` to the LLM client and `connect_timeout=10` to both the DB engine and the checkpointer's connection string; `graph.py`'s resume-retry now also catches `APITimeoutError`. Re-ran the exact Alice-balance flow from the live UI end to end afterward - correct account, correct balance, no hang. 46 tests still passing.
- 2026-09-11 — Switched the active LLM provider from Groq to Meta Muse Spark (`muse-spark-1.3-contributor`). Trigger: testing the new chat UI hit Groq's known transient tool-call parse failure (#12) for real - all 3 of `_invoke()`'s retries failed back-to-back on one request, exhausting the retry budget by design and surfacing as a raw 500 through `/chat` (the frontend showed "couldn't reach the server"). Rather than tune Groq's retry/backoff, switched to Muse Spark - already validated on the harder multi-turn tool-calling test the session before (PROBLEMS.md #18) - confirmed live via a real `/chat` call post-switch. `llm.py`'s docstring keeps the full provider history (why DeepSeek/Qwen3 were rejected, Groq's exact failure mode) in case a revert is ever needed. See PROBLEMS.md #19.
- 2026-09-11 — Closed out five pending items in one pass, per explicit direction. (1) Routing-accuracy eval deferred again — moved to a Backlog section instead of leaving it implicit. (2) DeepSeek re-tested a third time with the one thing both prior attempts missed (`extra_body={"thinking": {"type": "disabled"}}`, found via DeepSeek's own Thinking Mode docs) — the reasoning_content crash is gone, but a live multi-turn run silently hallucinated a completed account-opening with zero DB writes; rejected again as a worse failure mode than the crash, reverted to Groq (2 live calls). See PROBLEMS.md #17. (3+4) `delete_account` now cascades to related transactions/service requests instead of orphaning them, and the REST delete endpoint is gated by a new `admin_auth.require_admin` dependency (static shared-secret `X-Admin-Key` header) as an interim stand-in for Phase 6's real IAM. See PROBLEMS.md #16. (6) Confirmed the Session Store isn't actually a dead consumer — `touch`/`get_last_activity`/`delete` are exercised on every `/chat` call and by the new sweep; only the shared-state half was ever meant to have no consumer yet, by explicit Phase 4 design choice. (7) Implemented the previously-deferred idle-session sweep: `graph.sweep_expired_sessions()` on a timer via a FastAPI `lifespan` background task in `main.py`, closing the "abandoned session lingers forever" gap without adding a scheduler dependency. 4 new tests (cascade delete, admin-gate 403s, sweep), 46 passing total.
- 2026-09-12 — Reviewed the reference architecture diagram block-by-block against current state. Data-plane half (agents -> MCP servers -> DB, session continuity, narrow PII redaction) matches; control-plane/edge half doesn't - no WAF/DDoS/gateway, no observability/cost tracking despite running a paid LLM, and **no customer-facing Authentication at all** (flagged as the most serious gap: any anonymous caller could act on any account_id it named or guessed, over both `/chat` and REST).
- 2026-09-12 — Built Phase 7's first slice: in-app rate limiting (`slowapi`). New `rate_limit.py` (one shared `Limiter`, in-memory storage - single-process only, noted as a revisit item once there's more than one app process). `POST /auth/login` and `POST /chat` limited to 5/minute and 20/minute per IP respectively (IP is the only identity that exists for either surface). 2 new tests (`tests/test_rate_limiting.py`, zero LLM cost - the /chat test stubs `run()`), 69 passing total. Live curl confirmed 429 past each threshold.
- 2026-09-12 — Reversed Phase 6's "staff/admin only" scoping at explicit user direction: the Authentication gap from the deviation review was called unacceptable, so customer login/password was built even though it collides with the still-separately-deferred account-schema-redesign decision (see account-schema-redesign-deferred memory's 2026-09-12 update - this adds a credential + ownership link only, doesn't touch what `owner_name` means). Planned via EnterPlanMode with an Explore pass confirming the exact gap first (every REST endpoint except staff-gated DELETE was unauthenticated; `/chat`'s session_id has no identity binding at all). Built: `Customer` model + `customer_router.py` (self-service register/login, unlike staff), nullable `Account.customer_id` (one customer, many accounts - manual `ALTER TABLE`, no migration tool exists), `authz.py` (`require_owner` for REST, `owner_guard`/`inject_customer_id` tool-layer wrappers using the same `RunnableConfig`-injection technique as `pii_guard`/`idempotent`, verified empirically against LangChain's actual schema-inference behavior before committing to the design - a plain-named parameter is NOT auto-hidden the way `RunnableConfig`-typed ones are, so `create_account`/`list_accounts` needed an explicit `args_schema` to keep customer_id off the LLM-visible schema). JWTs now carry a `"typ"` claim distinguishing staff/customer tokens (previously indistinguishable). `graph.py`/`session_store.py`: bound a client-chosen `session_id` to whichever customer first uses it (`session_store.store`, the first real consumer of Phase 4's previously-unwired shared-state store) - a different customer reusing that id now gets 403 instead of silently sharing conversation state. New `list_accounts` chat tool + `GET /accounts` REST endpoint so a customer never has to already know an account id. 11 new tests across `test_customer_auth.py` (new) and updates to `test_rest_endpoints.py`/`test_auth.py`/`test_rate_limiting.py`, 80 passing total. Verified live: register, open an account, 401 with no token, 404 for a second customer trying another's account, correct per-customer account-list scoping, one `/chat` exchange per customer proving `list_accounts` answers "what's my balance" with zero account id stated, and confirmed session_id reuse across customers is rejected (403).
- 2026-09-13 — Built Phase 8 (Observability & Cost Tracker), the next fully unstarted phase (Phase 7's remainder is blocked on the still-undecided deployment target). Planned via EnterPlanMode: deliberately scoped to call *metadata* (timing, token usage, success/failure) not raw prompt/reply/tool-arg content - this app already tokenizes PII before it reaches the LLM (Phase 5), and a new unprotected log table storing conversation text would quietly reopen that concern. New `AgentEventLog` table + `observability.py`'s `ObservabilityCallbackHandler`, a standard LangChain `BaseCallbackHandler` (the same extension point LangSmith tracing uses) attached once via `graph.py`'s invoke config - no changes needed anywhere else (`llm.py`, any `*_tools.py`, the agents). New staff-gated `GET /observability/usage` aggregates totals + a by-day breakdown. Verified two things empirically rather than assuming, per the plan: LangChain does not pass `metadata` to `*_end`/`*_error` callbacks (only `*_start`) - fixed by capturing context at start, keyed by `run_id`; and `agent_name` attribution isn't reliably available from LangGraph's supervisor setup at all - a real live call recorded `agent_name: None` on every row, reported honestly rather than claimed working. That same live call's real data immediately surfaced a genuine anomaly - an 85.7s LLM call against a configured 30s client timeout - which is exactly the kind of thing this phase exists to make visible; logged, not chased down mid-phase. See PROBLEMS.md #23. 10 new tests (zero LLM cost, synthetic objects fed directly to the handler + a REST test), 96 passing total.
- 2026-09-13 — Completed Phase 7 (Edge Layer): the deployment target (bare VPS, self-managed Docker Compose) was decided, unblocking the part of Phase 7 that had been waiting on it. Explicitly followed an established reference architecture rather than improvising, per direction: researched FastAPI's own official `full-stack-fastapi-template` (its documented "FastAPI + Traefik + Docker Compose" pattern) and Traefik's v3 reference docs directly (RateLimit/Headers middleware option names, ACME challenge-type tradeoffs) rather than guessing at syntax. Adopted the reference's key choices: an external `traefik-public` Docker network, single-domain path-based routing (decided with you: restructure every backend route under a shared `/api` prefix - matches the reference exactly and makes frontend/backend same-origin, eliminating CORS in production - over the cheaper-but-nonstandard alternative of enumerating today's path prefixes in a Traefik rule), explicit per-service routing opt-in, and `tlsChallenge` for Let's Encrypt. Two prerequisites surfaced by exploration and fixed: the backend had never been containerized (new `Dockerfile`, run via `fastapi run` not `fastapi dev`), and `database.py`'s `DATABASE_URL` was a hardcoded literal that silently ignored `.env`'s own line - harmless on a single host, but would have broken the containerized backend's ability to reach `db` by service name. Restructured every router under `/api` in `main.py` via one parent `APIRouter` (each domain router's own internal prefix untouched) - updated every REST/customer/rate-limit/observability test's paths accordingly, and the frontend's `VITE_API_BASE_URL` default (no frontend source changes needed). New `docker-compose.prod.yml` (additive, `docker-compose.yml` stays local-dev-only) wires `db`/`backend`/`frontend`/`traefik` together, with security-headers and centralized rate-limiting middleware on the backend router - the fix for `slowapi`'s already-flagged in-memory/per-process limitation (kept, not removed - defense in depth). Verified what's verifiable locally: 96 tests passing, backend Docker image builds and runs, compose file parses cleanly, HTTP→HTTPS entrypoint redirect works, and a full live round trip (login → open account → `/api/chat`) through the new `/api` prefix. **Could not verify Traefik's Docker-provider routing itself** - a genuine Windows/Docker Desktop socket-forwarding incompatibility with Traefik's client library (isolated and confirmed not a config/mount problem), reported honestly rather than claimed working; needs the real VPS. See PROBLEMS.md #24.
- 2026-09-13 — Closed the service-request self-approval gap, left open from an earlier live-usage question ("who approves this?"). Ownership (Phase 6) and the state machine's legal-transition check (`service_server.py`, pre-existing) were both already enforced, but neither ever checked *who* may trigger a status transition - any customer could approve/reject/complete their own request. Removed `status` from the customer-facing REST PATCH (`service_router.py`) and the chat tool (`service_tools.py` - `_update_service_request_details` is now details-only) entirely, so it can't be reintroduced through a prompt change later; added a new staff-only `PATCH /staff/service-requests/{id}/status`, gated by the existing `get_current_staff_user` dependency. `service_server.py`'s state machine itself is unchanged - it still owns which transitions are legal, just not who may trigger one. Verified live (not just via tests): a real customer PATCH with `status` now silently no-ops on status; the same customer's token against the staff endpoint gets 401; a real staff token successfully approves. 3 REST tests added/updated, 99 passing total. See PROBLEMS.md #25.
